# (Moved) Unity project context

Since 2026-09-24 the project context lives in a single entry point and a documentation tree.

- Start with [`HANDOFF.md`](../../HANDOFF.md) at the repository root: current state, what works, next tasks, absolute rules and the document map.
- The tree is described in [`Docs/README.md`](../README.md).

The previous text is in git history: `git show 01dd655:Docs/AI/UnityProjectContext.md`.
